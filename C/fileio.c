#include <stdio.h>
#include <math.h>
#include <string.h>
#include <stdlib.h>


int main(){
    // fscanf(stdin, <format string>) // number of conversions (EOF if end of file or error)
    // fprintf(stdout,<format string>) // -ve is error
    // FILE * - I/O stream
    // fopen("path", "r")
    // r -read
    // w-write ( it destroys whatever existed in file and creates a new one if file doesn't exist)
    // a-append  ( it keeps whatever existed in file)
    // fclose("path"); returns 0 if success 
    // once closed then only file can be read
    // technically nothing goes wrong if we don't close the file 
return 0; 
}